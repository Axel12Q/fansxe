(() => {
    const mode = document.body.dataset.auth;
    const register = mode === 'register', recovery = mode === 'recovery';
    const title = recovery ? 'Recupera tu acceso' : register ? 'Tu comunidad empieza aquí' : 'Qué bueno verte de nuevo';
    document.title = (recovery ? 'Recuperar contraseña' : register ? 'Crear cuenta' : 'Iniciar sesión') + ' · Fansxe';
    document.getElementById('auth-content').innerHTML = `<aside class="auth-art"><span class="eyebrow" style="color:#ddd6fe">Conecta. Comparte. Crea.</span><h2>Un espacio para ser tú.</h2><p>Tus ideas, tus historias y las personas que quieren acompañarte.</p><img src="https://images.unsplash.com/photo-1507525428034-b723cf961d3e?auto=format&fit=crop&w=800&q=80" alt="Playa al atardecer, inspiración para una nueva historia"></aside><section class="auth-card"><h1>${title}</h1><p>${recovery ? 'Escribe tu correo para preparar la solicitud de recuperación.' : register ? 'Crea tu cuenta de prueba y explora Fansxe.' : 'Entra para descubrir lo nuevo de tu comunidad.'}</p>${recovery ? '' : '<button type="button" class="google-button" data-google><b aria-hidden="true">G</b> Continuar con Google</button><div class="auth-divider">o con tus datos</div>'}<form id="auth-form" data-mode="${mode}">${register ? '<label class="form-label" for="auth-username">Tu username</label><input id="auth-username" name="username" class="text-field" autocomplete="username" minlength="3" maxlength="20" pattern="[A-Za-z0-9_]{3,20}" placeholder="tu_usuario" required><p class="field-help">Tu @usuario: de 3 a 20 letras, números o guiones bajos.</p><label class="form-label" for="auth-name">Tu nombre</label><input id="auth-name" class="text-field" name="name" autocomplete="name" maxlength="60" required>' : ''}<label class="form-label" for="auth-email">${mode === 'login' ? 'Correo o usuario' : 'Correo electrónico'}</label><input id="auth-email" class="text-field" name="email" type="${mode === 'login' ? 'text' : 'email'}" autocomplete="${mode === 'login' ? 'username' : 'email'}" maxlength="254" required placeholder="${mode === 'login' ? 'Correo o @usuario' : 'tu@correo.com'}">${recovery ? '' : `<label class="form-label" for="auth-password">Contraseña</label><div class="password-field"><input id="auth-password" class="text-field" name="password" type="password" autocomplete="${register ? 'new-password' : 'current-password'}" ${register ? 'minlength="8"' : ''} maxlength="128" required><button type="button" data-password-toggle>Mostrar</button></div>${register ? '<label class="form-label" for="auth-confirm">Repetir contraseña</label><input id="auth-confirm" class="text-field" name="confirm" type="password" autocomplete="new-password" minlength="8" maxlength="128" required><p class="field-help">Usa al menos 8 caracteres.</p><label class="auth-age" for="auth-adult"><input id="auth-adult" name="adult" type="checkbox" required><span>Confirmo que tengo 18 años o más.</span></label><p class="field-help">Esta declaración no sustituye la verificación de edad para publicar contenido privado.</p>' : '<a class="auth-help" href="recuperar.html">¿Olvidaste tu contraseña?</a>'}`}<button type="submit" class="public-primary auth-submit">${recovery ? 'Solicitar correo de recuperación' : register ? 'Crear mi cuenta' : 'Iniciar sesión'} →</button></form><p id="auth-status" role="status" aria-live="polite"></p><div class="auth-bottom">${recovery ? '<a href="login.html">Volver a iniciar sesión</a>' : register ? '¿Ya tienes cuenta? <a href="login.html">Inicia sesión</a>' : '¿Primera vez aquí? <a href="registro.html">Crear cuenta</a>'}</div><div class="auth-demo">${mode === 'login' ? 'Acceso de prueba: <strong>admin / admin</strong>.<br>' : ''}Demo local: las cuentas comparten los datos de este navegador. Google y el envío de correos aún no están conectados. Usa datos de prueba.</div></section>`;
})();
